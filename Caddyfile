neshoone.smarbiz.sbs, neshoone.hamooncloud.ir {
    encode zstd gzip
    reverse_proxy app:8000

    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        Referrer-Policy no-referrer
        Permissions-Policy "camera=(), microphone=(), geolocation=()"
    }
}
